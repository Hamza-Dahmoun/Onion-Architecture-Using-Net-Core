@model CompanyName.AppName.Domain.Entities.Activity

@{
    ViewData["Title"] = labelLocalizer["Details"];
}

<h1>@labelLocalizer["Details"]</h1>

<div>
    <h4>@labelLocalizer["Activity"]</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Code)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Code)
        </dd>
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.Description)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.Description)
        </dd>
    </dl>
</div>
<div>
    <a asp-action="Edit" asp-route-id="@Model.Id">@labelLocalizer["Edit"]</a> |
    <a asp-action="Index">@labelLocalizer["Back to List"]</a>
</div>
